{
  "name": "cff-planning-poker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --prefix backend run dev\" \"pnpm --prefix frontend run dev\"",
    "build": "pnpm --prefix frontend run build && pnpm --prefix backend run build",
    "start": "node backend/dist/index.js",
    "preview": "pnpm --prefix frontend run preview"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
